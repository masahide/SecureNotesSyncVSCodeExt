{
  "name": "secure-notes-sync",
  "displayName": "Secure Notes Sync",
  "description": "A VSCode extension for securely syncing notes with a GitHub repository.",
  "version": "2026.3.9052231",
  "publisher": "MasahideYAMASAKI",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/masahide/SecureNotesSyncVSCodeExt.git"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "workspaceContains:.secureNotes/wsIndex.json"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "secureNotesBranches",
          "title": "Secure Notes Branches",
          "icon": "./assets/secure-note.svg"
        }
      ]
    },
    "views": {
      "secureNotesBranches": [
        {
          "id": "secureNotesBranchesView",
          "name": "Branches"
        }
      ],
      "explorer": [
        {
          "id": "secureNotes.branchList",
          "name": "Branches"
        },
        {
          "id": "secureNotes.indexHistory",
          "name": "Index History"
        }
      ]
    },
    "menus": {
      "view/item/context": [
        {
          "command": "secureNotes.createBranchFromIndex",
          "when": "view == secureNotesBranchesView && viewItem == indexItem"
        },
        {
          "command": "secureNotes.checkoutBranch",
          "when": "view == secureNotesBranchesView && viewItem == branchItem"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Secure Notes Sync",
      "properties": {
        "SecureNotesSync.gitRemoteUrl": {
          "type": "string",
          "description": "GitHub repository URL for syncing the object directory. (ex: git@github.com:user/repo.git)",
          "default": "git@github.com:user/repo.git"
        },
        "SecureNotesSync.enableAutoSync": {
          "type": "boolean",
          "description": "Enable auto sync on save",
          "default": false
        },
        "SecureNotesSync.inactivityTimeoutSec": {
          "type": "number",
          "description": "Inactivity timeout in seconds for triggering sync when the window is refocused",
          "default": 60
        },
        "SecureNotesSync.saveSyncTimeoutSec": {
          "type": "number",
          "description": "Timeout in seconds after file save before triggering sync",
          "default": 5
        },
        "SecureNotesSync.onePasswordUri": {
          "type": "string",
          "description": "If set to an op://… URI, the AES encryption key will be retrieved via 1Password CLI",
          "default": ""
        },
        "SecureNotesSync.onePasswordAccount": {
          "type": "string",
          "description": "(Optional) The 1Password account name to use with the op CLI",
          "default": ""
        },
        "SecureNotesSync.onePasswordCacheTimeout": {
          "type": "string",
          "description": "Cache timeout for the AES encryption key retrieved via 1Password (e.g., '1h', '30m', '2d')",
          "default": "30d"
        }
      }
    },
    "commands": [
      {
        "command": "secureNotes.sync",
        "title": "SecureNotes: Sync"
      },
      {
        "command": "secureNotes.initializeNewStorage",
        "title": "SecureNotes: Initialize New Storage"
      },
      {
        "command": "secureNotes.importExistingStorage",
        "title": "SecureNotes: Import Existing Storage"
      },
      {
        "command": "secureNotes.previewIndex",
        "title": "SecureNotes: Preview Index"
      },
      {
        "command": "secureNotes.createBranchFromIndex",
        "title": "SecureNotes: Create Branch from Index"
      },
      {
        "command": "secureNotes.checkoutBranch",
        "title": "SecureNotes: Checkout Branch"
      },
      {
        "command": "secureNotes.setAESKey",
        "title": "SecureNotes: Set AES Key"
      },
      {
        "command": "secureNotes.generateAESKey",
        "title": "SecureNotes: Generate AES Key"
      },
      {
        "command": "secureNotes.copyAESKeyToClipboard",
        "title": "SecureNotes: Copy AES Key to Clipboard"
      },
      {
        "command": "secureNotes.refreshAESKey",
        "title": "SecureNotes: Refresh AES Key"
      },
      {
        "command": "secureNotes.insertCurrentTime",
        "title": "SecureNotes: Insert Current Time"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "typecheck": "tsc -p . --noEmit",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "xvfb-run -a node ./out/test/run-all-tests.js",
    "test:headless": "xvfb-run -a node ./out/test/run-all-tests.js",
    "test:local": "node ./out/test/run-all-tests.js",
    "test:sync": "npm run compile-tests && npm run compile && xvfb-run -a node ./out/test/alternative-test-runner.js",
    "test:incremental": "npm run compile-tests && npm run compile",
    "test:unit": "npm run compile-tests && node ./out/test/unit-test.js",
    "test:fast": "npm run test:unit && npm run lint",
    "format": "prettier --write .",
    "verify": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "@types/node": "~25.3.5",
    "@types/vscode": "^1.109.0",
    "@types/which": "^3.0.4",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^10.0.3",
    "prettier": "^3.8.1",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.105.4",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "uuid": "^13.0.0",
    "which": "^6.0.1"
  }
}
